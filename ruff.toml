target-version = "py312"
line-length = 100
src = [".", "test"]

[lint]
select = ["E", "F", "I", "W", "UP", "B"]
# B027: optional no-op lifecycle hooks (close) on ABCs are intentional.
ignore = ["B027"]
